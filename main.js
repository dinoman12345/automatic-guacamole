function setup(){
    canvas=createCanvas(650,530);
    canvas.position(110,250)
    capture=createCapture(VIDEO);
    capture.hide();
    x="";
}
function draw(){
    image(capture,0,0,650,530);
    tint(x);
}
function gfg(){
    x=document.getElementById("chsadash").value;
}
function take_snapshot(){
    save('dino_are_winners_only_choose_green')
}





























































































































































































































































































































































































































































































































































































































































































































































































































































































